## docker search命令&
说明：在docker hub中搜索镜像

### 用法
```
Usage:	docker search [Options] TERM
```

| 选项 | 说明 |
| --- | --- |
| --limit int | 限制搜索结果数量 |
| --no-trunc | 不截断输出 |

### 示例
```shell
# 搜索nginx镜像
docker search nginx
```
---