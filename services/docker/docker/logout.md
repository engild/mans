## docker logout命令&
说明：从一个Docker registry服务器登出。


### 用法
```
docker logout [SERVER]

如果不指定server，刚从默认的registry服务器登出。
```

### 示例
```sh
# 从myregistry.example.com登出
docker logout myregistry.example.com

```
